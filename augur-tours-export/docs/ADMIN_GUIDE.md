# Admin Guide

Use `/admin`, `/admin/hotels`, `/admin/partners`, `/admin/car-hire-requests`, and `/admin/gallery` to manage business operations. Connect these screens to authenticated backend storage before production.
